services:
  agenta:
    build:
      context: ../..              # src/ directory as build context
      dockerfile: AI_APP/agentA/Dockerfile  # Path to Dockerfile from src/
    image: agenta:latest
    container_name: agenta
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env

