# AI_APP Unified Environment Variables

# --- Kafka Configuration ---
# Using IP from Agent A's .env which seems to be the active one
KAFKA_BOOTSTRAP=10.255.32.110:9092

# --- Gate Identification ---
GATE_ID=1

# --- NGINX RTMP Streaming Configuration ---
NGINX_RTMP_HOST=10.255.32.80
NGINX_RTMP_PORT=1935

# --- MinIO Object Storage ---
# Agent A had .70, Agent B had .82. Using .70 as base, but this should be verified.
# However, B and C use .70 in their .env but .82 in example. I'll stick to what was in the active .env files (.70)
MINIO_HOST=10.255.32.70
MINIO_PORT=9000
ACCESS_KEY=admin
SECRET_KEY=adminadmin

# --- Agent Specific Configs ---
BUCKET_NAME_LP=lp-crops
BUCKET_NAME_HZ=hz-crops

# Model download paths
MODELS_PATH_A=/app/AI_APP/agentA/data
MODELS_PATH_B=/app/AI_APP/agentB/data
MODELS_PATH_C=/app/AI_APP/agentC/data
