services:
  jenkins:
    build: .
    container_name: jenkins
    restart: unless-stopped
    
    # Run as root to avoid permission issues with Docker socket
    user: root
    
    # Use host network to access VMs directly
    network_mode: host
    
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock  # Allow Jenkins to run Docker
      - /home/pei_user/jenkins/ssh_keys:/var/jenkins_home/.ssh  # Chaves SSH persistentes
      - /home/pei_user/jenkins/ssh_keys:/root/.ssh  # Docker CLI usa /root/.ssh
    
    environment:
      - JAVA_OPTS=-Djenkins.install.runSetupWizard=true
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/login"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

volumes:
  jenkins_home:
    driver: local

# Jenkins will be accessible at http://<host-ip>:8080
# (or http://localhost:8080 from the VM itself)
#
# After starting, get initial password with:
# docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
