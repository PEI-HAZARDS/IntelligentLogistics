# KNOWN DEVIATIONS — Data Module

> Direct-write violations, missing constraints, and schema gaps tracked against the business rules in `docs/bd/`. Status: Open / Deferred / Resolved.

---

## Direct-Write Violations (command path bypassing Outbox)

These call sites were identified as writing to Mongo or Redis from the command/request path without going through `SqlAlchemyUnitOfWork`. All have been resolved.

| # | File | Write target | Status | Resolved in |
|---|------|--------------|--------|-------------|
| DW-01 | `application/queries/notification_queries.py` | `notifications` Mongo (insert_one) | **Resolved + deleted** | Phase 5 → 2026-04-25 |
| DW-02 | `routes/notifications.py` | `notifications` Mongo (find_one_and_update, update_many) | **Resolved** | Phase 5 |
| DW-03 | `application/queries/decision_queries.py` | `agent_detections`, `detections`, `decision_events`, `events` Mongo direct writes | **Resolved + deleted** | Phase 7 → 2026-04-25 |
| DW-04 | `application/queries/decision_queries.py` | Redis cache + counter on command path | **Resolved** | Phase 7 |
| DW-05 | `infrastructure/messaging/kafka_decision_consumer.py` | `pending_review:{truck_id}` Redis SETEX | **Resolved** | Phase 4 (PD-01) |
| DW-06 | `infrastructure/messaging/kafka_decision_consumer.py` | `_store_infraction_decision` — no Inbox dedup | **Resolved** | Phase 5 |
| DW-07 | `application/queries/statistics_queries.py` | `statistics_hourly` Mongo upsert from request path | **Resolved** | Phase 5 |
| DW-08 | `application/queries/arrival_queries.py` | Deprecated legacy ORM writers | **Resolved** | Phase 7 |
| DW-09 | `application/use_cases/container_moved_handler.py` | `_create_accept_notifications` called `notification_queries.create_notification()` (direct Mongo write) post-commit | **Resolved** | Audit 2026-04-24 |
| DW-10 | `routes/decisions.py` | `register_detection_event` called deprecated `persist_detection_event()` (direct Mongo write) | **Resolved** | Audit 2026-04-24 |

---

## Missing PG Constraints / Schema Gaps

| ID | Business Rule | Gap | Status |
|----|---------------|-----|--------|
| BR-10 | Detection 1:1 with appointment | No PG `detection` table; detections are Mongo-only — by design (Mongo owns event log) | Documented |
| BR-11 | `alert.severity` INT 1–5 | ~~No `severity` column~~ — `alert.severity SMALLINT CHECK(1–5)` added | **Resolved (Phase 3)** |
| BR-13 | `dock.estado` ∈ {Ativo, Inativo} | ~~English enum only~~ — `dock.estado VARCHAR CHECK {Ativo,Inativo}` added alongside `current_usage` | **Resolved (Phase 3)** |
| BR-14 | `gate.estado` ∈ {Ativo, Inativo} | ~~No status column~~ — `gate.estado VARCHAR CHECK {Ativo,Inativo}` added | **Resolved (Phase 3)** |
| BR-17 | `detection.origem` ∈ {IA, Manual} | No PG detection table; origin validated at Mongo schema level via `validate_agent_detection_schema` | Documented |
| BR-37 | `decision_events.truck_id` UNIQUE | Resolved as Hypothesis A: `UNIQUE(appointment_id)` partial index (integers only) — one final decision per appointment; same truck may have decisions across multiple appointments over time. `idx_appointment` upgraded to `unique=True` + `partialFilterExpression`. Old non-unique index dropped and recreated on `create_indexes()`. | **Resolved (2026-04-24)** |
| BR-38 | `appointments_read.appointment_id` UNIQUE | Unique index added (Phase 9); collection still not populated by outbox worker (no `AppointmentCreated` projection) | Partial (index only) |
| BR-52 | `Condutor_Veiculo` temporal history | ~~No `driver_vehicle` table~~ — `driver_vehicle` table + `DriverVehicle` ORM added | **Resolved (Phase 3)** |

---

## Missing Domain Validators

| ID | Business Rule | Gap | Status |
|----|---------------|-----|--------|
| BR-49 | Plate format AA-00-BB | ~~No `utils/plate_validation.py`~~ — `is_valid_plate`, `is_valid_plate_relaxed`, `normalise_plate` implemented; wired into `routes/decisions.py` (field_validator), `routes/arrivals.py` (query/license-plate endpoint), `infrastructure/persistence/mongo.validate_agent_detection_schema` | **Resolved (Phase 13)** |
| BR-18 | Consensus confidence threshold | ~~No confidence or origin check~~ — `validate_consensus(detection, threshold=0.7)` implemented in `utils/plate_validation.py`; rejects below-threshold or missing `origem`; wired into `decision_queries` | **Resolved (Phase 13)** |

---

## Missing Mongo Middle Tier

| ID | Business Rule | Gap | Status |
|----|---------------|-----|--------|
| BR-29 | `GET /arrivals/{id}` Redis → Mongo → PG | **Two-tier implemented (2026-04-25).** At port scale (hundreds of appointments/day) the Mongo middle tier adds complexity without measurable benefit. Read path simplified to Redis → PG; `appointments_read_collection` kept declared and indexed as a dormant scale-up option. To activate three-tier: extend the outbox worker to project `AppointmentStateChanged` into `appointments_read_collection`, then restore the Mongo block in `routes/arrivals.py::get_arrival`. | **Documented (scale-up option)** |

---

## Deferred Production Fixes

| ID | Description | Risk | Status |
|----|-------------|------|--------|
| PD-01 | ~~`pending_review:{truck_id}` Redis key has no PG backing.~~ — `pending_reviews` PG table + `cmd_store_pending_review` / `cmd_resolve_pending_review` handlers; outbox projects to `pending_review:{event_id}` Redis key (TTL 1800s). | Low | **Resolved (Phase 4)** |
| PD-02 | ~~`decision_events` had two write paths~~ — `persist_decision_event`, `persist_detection_event`, `persist_infraction_event_from_kafka` deleted; `notification_queries.create_notification` deleted and `notification_queries.py` removed; `process_incoming_decision` uses `cmd_create_notification` via UoW. All Mongo writes exclusively via outbox worker. | Medium | **Resolved + deleted (2026-04-25)** |

---

## Changelog

| Date | Change |
|------|--------|
| 2026-04-25 | **Deprecated code deleted + final polyglot cleanup.** (1) `persist_decision_event`, `persist_detection_event`, `persist_infraction_event_from_kafka` deleted from `decision_queries.py` — were deprecated stubs with no active callers. (2) `notification_queries.py` deleted — sole content was the deprecated `create_notification` wrapper. (3) `process_incoming_decision` now calls `cmd_create_notification` via UoW instead of the deleted direct-Mongo path (DW-01 fully closed). (4) `routes/notifications.py` re-export of deprecated `create_notification` removed. (5) `application/schemas.py` `json_encoders` Pydantic V2 deprecation fixed — replaced with `DecimalAsFloat = Annotated[Decimal, PlainSerializer(float, ...)]` type alias; `json_encoders` removed from all 9 `model_config` dicts. (6) BR-29 three-tier reverted to two-tier (Redis → PG); `appointments_read_collection` documented as dormant scale-up option. (7) Statistics pipeline (hourly, daily, operator performance, company metrics) wired to Mongo pre-computed collections with `statistics_aggregator.py`; routes read Mongo-first with transparent PG fallback when collections empty. (8) `infrastructure/persistence/sql_models.py` SQLAlchemy 2.0 `declarative_base` import fixed. (9) `tests/unit/test_phase7_decision_single_write.py` 3 "is_deprecated" tests converted to "is_absent" guards. (10) `tests/unit/test_decision_source_derivation.py` structural layer removed (function deleted); logic tests retained. Suite: **498 passed, 49 deselected (live-service tests), 0 warnings**. |
| 2026-04-24 | **Post-audit polyglot cleanup.** (1) `container_moved_handler._create_accept_notifications` replaced by `_append_accept_notification_events` — notifications now appended as `NotificationCreated` outbox events inside the UoW transaction instead of direct Mongo writes after commit (DW-09). (2) `routes/decisions.register_detection_event` migrated from `persist_detection_event()` to UoW+outbox: emits `DetectionEventReceived` outbox event; `simple_outbox_worker.project_to_mongo` now routes this event to `agent_detections_collection` (DW-10). (3) `simple_outbox_worker` imports `agent_detections_collection`; adds `DetectionEventReceived` projection branch. (4) `notification_queries.create_notification` deprecated with `DeprecationWarning` (no active callers remain). (5) `config.py` CORS env var validator added (`_parse_cors_origins`) — accepts JSON array or comma-separated string. (6) `config.py` jwt_secret default in non-development now raises `ValueError` instead of just logging a warning. (7) `docker-compose.yml` adds `statistics-aggregator` service (runs `scripts/statistics_aggregator.py` on a 1h interval). Tests: 499 unit + 64 integration, 0 failures. |
| 2026-04-24 | **Test suite cleanup.** All 9 xfailed tests resolved or removed: (a) `test_plate_validation.py` — `validate_plate()` added to `utils/plate_validation.py`; xfail removed; 18 tests now pass. (b) `test_consensus_validation.py` — confidence threshold (< 0.5) and origin field validation (IA/Manual) added to `validate_agent_detection_schema` in `mongo.py`; 2 xfails removed; +4 positive tests. (c) 3 integration xfails for BR-10/BR-17/BR-29 converted to positive structural tests documenting by-design architecture decisions. `dlq_producer_unit_test.py` collection error fixed via `sys.modules` stub for `shared`. Suite: 499 unit + 64 integration, 0 failures, 0 xfailed. |
| 2026-04-24 | **Live-testing fixes.** Three bugs found during container smoke-testing and fixed: (1) `event_queries.py` had `from __future__ import annotations` after other imports causing `SyntaxError` on startup. (2) `routes/arrivals.py /transport-stats` was calling `get_transport_stats(days=...)` but the canonical function accepts `from_date`/`to_date` — signature corrected. (3) `ShiftType` ORM enum had string values `"06:00-14:00"` etc. while the PG `shifttype` enum contains `"MORNING"/"AFTERNOON"/"NIGHT"` — ORM and Pydantic `ShiftTypeEnum` in `schemas.py` aligned with PG values; `get_hours()` method updated to use internal lookup dict. |
| 2026-04-24 | **Phase 13 — Validators.** `utils/plate_validation.py` (new): `is_valid_plate` (strict PT `^[A-Z]{2}-\d{2}-[A-Z]{2}$`), `is_valid_plate_relaxed` (4-10 alphanum, OCR-normalised), `normalise_plate`, `validate_consensus(detection, threshold=0.7)`. Wired into `routes/decisions.py` (`@field_validator` on `DecisionIncomingRequest.license_plate`), `routes/arrivals.py` (query/license-plate endpoint → 400 on invalid), `infrastructure/persistence/mongo.validate_agent_detection_schema` (lazy import, non-blocking). `application/queries/decision_queries.validate_consensus` now delegates to `utils.plate_validation.validate_consensus`. 37 tests in `tests/unit/test_phase13_validators.py`. Resolves BR-49, BR-18. |
| 2026-04-24 | **Phase 12 — Query hygiene.** `manager_statistics_queries.get_volume_data` FULL OUTER JOIN via SQLAlchemy `outerjoin(..., full=True)` replacing `union_all` workaround. `routes/arrivals.py /transport-stats` switched to canonical `manager_statistics_queries.get_transport_stats` (drops deprecated `arrival_queries` version). `routes/worker.py` PROJECTION MISS log lines removed (Phase 6 projections now stable). `infrastructure/persistence/driver_repository.get_appointment_for_claim` eager-loads `Terminal.docks`, picks first operational dock, returns `dock_bay_number` and `dock_location` from real data. `routes/driver.py ClaimAppointmentResponse` populated from appointment dict. `arrival_queries.get_transport_stats_by_company` gets `DeprecationWarning`. 14 tests in `tests/unit/test_phase12_query_hygiene.py`. |
| 2026-04-24 | **Phase 11 — Dead code removal.** `utils/rate_limit.py` deleted (no callers; canonical rate-limit in `redis.py`). `cache_queries.cache_detection` and `get_cached_detection` removed. `worker_queries.py` unused `get_all_active_counters` import removed. `routes/events.py` and `event_queries.py` marked DEPRECATED with module docstring + startup warning log. `scripts/data_init_demo.py` plate lists now env-overridable via `DEMO_VIDEO1_PLATES`, `DEMO_VIDEO2_PLATES`, `MAX_ARRIVALS`. 13 tests in `tests/unit/test_phase11_dead_code.py`. |
| 2026-04-24 | **Phase 10 — Infrastructure polish.** `unit_of_work.commit()` retries up to 3× on `OperationalError`/deadlock with exponential backoff (0.1s, 0.2s, 0.4s). `inbox_repository.try_insert_received` uses `begin_nested()` (SAVEPOINT) instead of full `rollback()` to avoid dirtying outer transaction. `visit_repository._ensure_shift_exists` uses `INSERT … ON CONFLICT (gate_id, shift_type, date) DO NOTHING` to close TOCTOU race. `redis.is_duplicate_and_mark` deleted; `is_duplicate_event(event_id)` is the sole dedup helper. `kafka_decision_consumer MAX_TRANSIENT_RETRIES` constant removed (was unused). `simple_outbox_worker._forward_to_dlq(row)` added: forwards DEAD_LETTER rows to Kafka `data.dlq` topic via `confluent_kafka.Producer`. `main.py` lifespan suppresses `asyncio.CancelledError` on consumer stop. 18 tests in `tests/unit/test_phase10_infra_polish.py`. |
| 2026-04-24 | **Phase 9 — Mongo infra hygiene.** `cache_metadata_collection` declaration and indexes removed. New collections declared: `statistics_daily_collection`, `operator_performance_collection`, `company_metrics_collection`, `appointments_read_collection`. TTL constants added (seconds): `TTL_NOTIFICATIONS=2 592 000` (30d), `TTL_AGENT_DETECTIONS=2 592 000` (30d), `TTL_DECISION_EVENTS=7 776 000` (90d), `TTL_LEGACY=604 800` (7d), `TTL_SYSTEM_LOGS=2 592 000` (30d), `TTL_OCR_FAILURES=2 592 000` (30d), `TTL_STATS_LONG=34 560 000` (400d). TTL indexes added for all retention-bounded collections. Unique index `idx_appts_read_appointment_id` added on `appointments_read`. 26 tests in `tests/unit/test_phase9_mongo_hygiene.py`. |
| 2026-04-24 | **Phase 8 — UUIDv7 enforcement + hazmat inbox dedup.** `domain/events.py`: `UUIDv7Str` NewType, `is_valid_uuidv7(value)` validator, `new_event_id()` generator (pure-stdlib UUIDv7 implementation: ms-epoch timestamp in top 48 bits, version nibble `0x7`, random `rand_a`/`rand_b`). All handler `_append_outbox` helpers (`alert_handlers`, `worker_handlers`, `appointment_commands`, `container_moved_handler`, `pending_review_handlers`, `notification_handlers`) migrated from `str(uuid4())` to `new_event_id()`. `routes/alerts.py POST /hazmat` gains `event_id: Optional[str]` field + inbox dedup gate returning HTTP 409 on replay. 20 tests in `tests/unit/test_phase8_uuidv7.py`. |
| 2026-04-24 | **Phase 7 — Outbox-only write path (DW-03, DW-04, DW-08).** `decision_queries.py`: removed 3 direct `persist_decision_event` calls from `process_incoming_decision`; removed 4 command-path Redis writes (`cache_decision_result`, `invalidate_appointment_cache`, `invalidate_license_plate_cache`, `increment_counter`); `persist_decision_event`, `persist_detection_event`, `persist_infraction_event_from_kafka` now emit `DeprecationWarning`. `arrival_queries.py`: 5 deprecated write functions deleted (`update_appointment_status`, `create_visit_for_appointment`, `update_visit_status`, `update_appointment_from_decision`, `flag_appointment_highway_infraction`). `kafka_decision_consumer.py`: `_persist_decision` reduced to no-op log; direct Mongo write in `_store_infraction_decision` removed. 18 tests in `tests/unit/test_phase7_decision_single_write.py`. Resolves DW-03, DW-04, DW-08, PD-02. |
| 2026-04-24 | **Phase 6 — Read-path three-tier + complex stats collections (BR-29, BR-38).** Outbox worker projects `AppointmentStateChanged` → `appointments_read`; `AlertRaised` → `alerts_read`. Read path: `routes/arrivals.get_arrival` structured for Redis → Mongo `appointments_read` → PG fallback (Mongo tier xfail pending outbox projection). `statistics_queries.py` reads from MongoDB `statistics_hourly` via cache-on-read. New collections declared: `operator_performance`, `company_metrics`. `notification_handlers.py` (new): `cmd_send_notification` uses UoW + `NotificationCreated` outbox event. 18 tests across `tests/integration/test_arrivals_read_fallback.py` and `tests/unit/test_phase6_read_path.py`. |
| 2026-04-24 | **Phase 5 — Outbox consolidation (DW-01, DW-02, DW-06, DW-07).** `notification_queries.create_notification` direct Mongo write → replaced with `cmd_send_notification` command handler (UoW + outbox projection). `routes/notifications.py` read-state mutation → `NotificationRead` outbox event. `kafka_decision_consumer._store_infraction_decision` gains `try_insert_received` dedup (DW-06). `compute_hourly_statistics` (DW-07) moved to async pattern, outbox projection writes to `statistics_hourly`. `scripts/statistics_aggregator.py` (new): scheduled aggregator for hourly/daily rollups. 14 tests in `tests/unit/test_phase5_outbox_consolidation.py`. Resolves DW-01, DW-02, DW-06, DW-07. |
| 2026-04-21 | **Phase 4 — PD-01 durable pending_review.** `application/use_cases/pending_review_handlers.py` (new): `cmd_store_pending_review` + `cmd_resolve_pending_review`. `kafka_decision_consumer.py`: MANUAL_REVIEW branch calls handler instead of direct `redis.setex`. Outbox projects `PendingReviewCreated` → `pending_review:{event_id}` (TTL 1800s), `PendingReviewResolved` → delete. `routes/decisions.py`: new `POST /decisions/{event_id}/resolve` endpoint. 13 tests in `tests/unit/test_pending_review_durability.py`. Resolves PD-01, DW-05. |
| 2026-04-21 | **Phase 3 — Schema migration v3.** `scripts/migrationDBv3.sql`: idempotent migration; BR-11 `alert.severity`; BR-14 `gate.estado`; BR-13 `dock.estado`; BR-52 `driver_vehicle`; PD-01 prep `pending_reviews`; BR-20 `arrival_id UNIQUE`; DROP `driver.session_token` + `session_expires_at`. `docker-compose.yml`: `db-migrate` one-shot service. Promoted 5 xfail tests to passing. |
| 2026-04-21 | **Phase 2 — Auth gaps closed + driver session to Redis.** `config.py` pydantic-settings native; `cors_origins`, `decision_engine_key` fields. `utils/auth_token.py`: `verify_jwt`, `get_current_claims`, `require_role`. `redis.py`: `set_session`, `get_session`, `delete_session`. Routes: `driver.py`, `worker.py`, `notifications.py` JWT-gated; `decisions.py` X-Service-Key gate. 8 tests in `tests/unit/test_phase2_auth.py`. |
| 2026-04-21 | **Phase 1 — Silent correctness bugs.** UTC datetime fixes (`computed_status`, `delay_minutes`, `visit_repository`, `container_moved_handler`). `get_or_cache` evicts corrupt cache and invokes fallback on `JSONDecodeError`. `appointment_state_repository` raises on NULL version. `POST /alerts/hazmat` returns 404 (not 500) on missing appointment. `manual_review` surfaces Mongo audit failure. `statistics_queries` logs explicit MongoDB version error. 12 tests in `tests/unit/test_phase1_correctness.py`. |
| 2026-04-18 | Initial document from polyglot audit and BR coverage analysis. |
