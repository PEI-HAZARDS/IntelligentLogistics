# SCHEMA REFERENCE — Data Module

> Column-level reference for PostgreSQL tables, MongoDB collections, and Redis key patterns.

---

## PostgreSQL

### `company`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `nif` | VARCHAR(20) | PRIMARY KEY | Tax ID; enforces uniqueness (BR-01) |
| `name` | VARCHAR(200) | NOT NULL | — |
| `contact` | VARCHAR(50) | — | Phone/email contact |

### `driver`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `drivers_license` | VARCHAR(50) | PRIMARY KEY | License number (BR-02) |
| `company_nif` | VARCHAR(20) | FK → company.nif | (BR-07) |
| `name` | VARCHAR(100) | NOT NULL | — |
| `password_hash` | TEXT | — | bcrypt (BR-39, BR-40) |
| `mobile_device_token` | TEXT | — | Push notifications |
| `active` | BOOLEAN | DEFAULT TRUE | Soft-delete |
| `current_appointment_id` | INTEGER | — | Sequential delivery tracking (BR-41) |
| `created_at` | TIMESTAMP | server_default=now() | — |

> `session_token` and `session_expires_at` **dropped** in Phase 3 migration. Driver sessions now stored in Redis `session:driver:{license}` (TTL 3600s).

### `truck`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `license_plate` | VARCHAR(20) | PRIMARY KEY | Format AA-00-BB (BR-03, BR-49) |
| `company_nif` | VARCHAR(20) | FK → company.nif | — |
| `brand` | VARCHAR(100) | — | — |

### `worker`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `num_worker` | VARCHAR(20) | PRIMARY KEY | — |
| `name` | VARCHAR(200) | NOT NULL | — |
| `phone` | VARCHAR(50) | — | — |
| `email` | VARCHAR(200) | UNIQUE | (BR-04) |
| `password_hash` | TEXT | — | bcrypt (BR-39) |
| `active` | BOOLEAN | DEFAULT TRUE | — |
| `created_at` | TIMESTAMP | server_default=now() | — |

### `manager`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `num_worker` | VARCHAR(20) | PK + FK → worker.num_worker | (BR-05) |
| `access_level` | ENUM('admin','basic') | DEFAULT 'basic' | — |

### `operator`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `num_worker` | VARCHAR(20) | PK + FK → worker.num_worker | (BR-06) |

### `terminal`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | SERIAL | PRIMARY KEY | — |
| `name` | VARCHAR(200) | — | — |
| `latitude` | DECIMAL(10,8) | — | — |
| `longitude` | DECIMAL(11,8) | — | — |
| `hazmat_approved` | BOOLEAN | DEFAULT FALSE | — |

### `dock`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `terminal_id` | INTEGER | PK + FK → terminal.id | Composite PK |
| `bay_number` | VARCHAR(50) | PK | Composite PK |
| `current_usage` | ENUM('maintenance','operational','closed') | DEFAULT 'operational' | Operational status enum |
| `estado` | VARCHAR(10) | CHECK {Ativo,Inativo} DEFAULT 'Ativo' | Portuguese spec status (BR-13) |
| `latitude` | DECIMAL(10,8) | — | — |
| `longitude` | DECIMAL(11,8) | — | — |

### `gate`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | SERIAL | PRIMARY KEY | — |
| `label` | VARCHAR(100) | NOT NULL | — |
| `estado` | VARCHAR(10) | CHECK {Ativo,Inativo} DEFAULT 'Ativo' | Active/inactive flag (BR-14) |
| `latitude` | DECIMAL(10,8) | — | — |
| `longitude` | DECIMAL(11,8) | — | — |

### `shift`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `gate_id` | INTEGER | PK + FK → gate.id | Composite PK |
| `shift_type` | ENUM(ShiftType) | PK | MORNING/AFTERNOON/NIGHT |
| `date` | DATE | PK | Shift date |
| `operator_num_worker` | VARCHAR(20) | FK → operator.num_worker | — |
| `manager_num_worker` | VARCHAR(20) | FK → manager.num_worker | — |

### `booking`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `reference` | VARCHAR(50) | PRIMARY KEY | e.g. BOOK-0001 |
| `direction` | ENUM('inbound','outbound') | — | — |
| `created_at` | TIMESTAMP | server_default=now() | — |

### `cargo`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | SERIAL | PRIMARY KEY | — |
| `booking_reference` | VARCHAR(50) | NOT NULL, FK → booking.reference | — |
| `quantity` | DECIMAL(10,2) | NOT NULL | — |
| `state` | ENUM('liquid','solid','gaseous','hybrid') | NOT NULL | Physical state (BR-12) |
| `description` | TEXT | — | — |

### `appointment`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | SERIAL | PRIMARY KEY | — |
| `arrival_id` | VARCHAR(50) | INDEX | PIN; auto-generated by trigger (BR-20) |
| `booking_reference` | VARCHAR(50) | NOT NULL, FK → booking.reference | (BR-08) |
| `driver_license` | VARCHAR(50) | NOT NULL, FK → driver.drivers_license | (BR-08) |
| `truck_license_plate` | VARCHAR(20) | NOT NULL, FK → truck.license_plate | (BR-08) |
| `terminal_id` | INTEGER | NOT NULL, FK → terminal.id | (BR-08) |
| `gate_in_id` | INTEGER | FK → gate.id | Entry gate (BR-09) |
| `gate_out_id` | INTEGER | FK → gate.id | Exit gate; nullable (BR-09) |
| `scheduled_start_time` | TIMESTAMP | — | — |
| `expected_duration` | INTEGER | — | Minutes |
| `status` | ENUM(appointment_status) | DEFAULT 'scheduled' | {scheduled, in_transit, in_process, unloading, canceled, delayed, completed} (BR-15) |
| `version` | INTEGER | NOT NULL, DEFAULT 1 | Optimistic concurrency (BR-48, Guardrail 6) |
| `notes` | TEXT | — | — |
| `highway_infraction` | BOOLEAN | DEFAULT FALSE | (BR-42) |

**Triggers**: `trg_generate_arrival_id` (arrival_id via sequence), `trg_check_visit_completion` (auto-complete on visit out_time).

### `visit`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `appointment_id` | INTEGER | PK + FK → appointment.id | 1:1 with appointment (BR-10 partial) |
| `shift_gate_id` | INTEGER | NOT NULL | Composite FK to shift |
| `shift_type` | ENUM(ShiftType) | NOT NULL | Composite FK to shift |
| `shift_date` | DATE | NOT NULL | Composite FK to shift |
| `entry_time` | TIMESTAMP | — | — |
| `out_time` | TIMESTAMP | — | — |
| `state` | ENUM('not_started','unloading','completed') | DEFAULT 'unloading' | (BR-16) |

### `alert`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | SERIAL | PRIMARY KEY | — |
| `visit_id` | INTEGER | FK → visit.appointment_id | Nullable (alert may predate visit) |
| `appointment_id` | INTEGER | FK → appointment.id | Direct link for pre-visit alerts |
| `timestamp` | TIMESTAMP | server_default=now() | — |
| `image_url` | TEXT | — | MinIO blob URL |
| `type` | ENUM('generic','safety','problem','operational') | DEFAULT 'generic' | Alert category |
| `severity` | SMALLINT | CHECK(1–5) DEFAULT 3 | Severity level 1=critical … 5=info (BR-11) |
| `description` | TEXT | — | — |

### `shift_alert_history`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | SERIAL | PRIMARY KEY | — |
| `shift_gate_id` | INTEGER | Composite FK to shift | — |
| `shift_type` | ENUM(ShiftType) | Composite FK to shift | — |
| `shift_date` | DATE | Composite FK to shift | — |
| `alert_id` | INTEGER | NOT NULL, FK → alert.id | — |
| `last_update` | TIMESTAMP | server_default=now() | Auto-created by trigger on alert insert |

### `inbox_events`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `event_id` | VARCHAR | PRIMARY KEY or UNIQUE | Idempotency key (BR-21) |
| `topic` | VARCHAR | — | Kafka topic |
| `partition` | INTEGER | — | — |
| `offset` | BIGINT | — | — |
| `aggregate_type` | VARCHAR | — | e.g. "appointment" |
| `aggregate_id` | VARCHAR | — | — |
| `event_type` | VARCHAR | — | e.g. "ContainerMoved" |
| `event_version` | INTEGER | — | Schema version |
| `status` | VARCHAR | — | RECEIVED→PROCESSING→PROCESSED\|FAILED\|DEAD_LETTER (BR-22) |

### `outbox_events`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `id` | UUID | PRIMARY KEY | — |
| `topic` | VARCHAR | — | Kafka topic |
| `key` | VARCHAR | — | Partition key |
| `event_type` | VARCHAR | — | — |
| `payload` | JSONB | — | EventEnvelope payload |
| `status` | VARCHAR | DEFAULT 'PENDING' | PENDING→PUBLISHED\|FAILED\|DEAD_LETTER (BR-23) |
| `created_at` | TIMESTAMP | — | — |
| `published_at` | TIMESTAMP | — | — |
| `retry_count` | INTEGER | DEFAULT 0 | Max 5 retries (BR-24) |
| `error` | TEXT | — | Last error message |

### `driver_vehicle`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `driver_license` | VARCHAR(50) | PK + FK → driver.drivers_license | Composite PK |
| `truck_license_plate` | VARCHAR(20) | PK + FK → truck.license_plate | Composite PK |
| `start_date` | DATE | PK | Temporal start (composite PK) |
| `end_date` | DATE | — | Nullable; NULL = current assignment |

**Constraint**: `chk_dv_dates` `end_date IS NULL OR end_date >= start_date`. **UNIQUE(driver_license, truck_license_plate, start_date)** enforces no duplicate history rows. Added Phase 3 (BR-52).

### `pending_reviews`
| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| `event_id` | UUID | PRIMARY KEY | Correlation key (UUIDv7) |
| `truck_id` | VARCHAR | NOT NULL | Truck identifier |
| `gate_id` | INTEGER | NOT NULL | — |
| `license_plate` | VARCHAR | — | — |
| `payload` | JSONB | NOT NULL | Full agent decision payload |
| `status` | VARCHAR | DEFAULT 'PENDING' | PENDING → APPROVED \| REJECTED |
| `created_at` | TIMESTAMP | server_default=now() | — |
| `resolved_at` | TIMESTAMP | — | Nullable |
| `resolved_by` | VARCHAR | — | Operator num_worker |

**Purpose**: durable backing for operator review queue. Redis `pending_review:{event_id}` (TTL 1800s) acts as projection cache; rebuilt from this table on Redis flush. Added Phase 4 (PD-01).

---

## MongoDB

### `agent_detections`
Document shape (variable):
```json
{
  "detection_id": "det_gate1_...",
  "truck_id": "TRUCK-...",
  "agent_type": "AgentA|AgentB|AgentC",
  "gate_id": 1,
  "timestamp": "ISODate",
  "detection_data": { "license_plate": "AB-12-CD", "confidence": 0.92, ... },
  "processing": { "consumed_by_decision_engine": false, ... }
}
```
**Indexes**: `idx_detection_id` (UNIQUE), `idx_truck_timestamp`, `idx_agent_gate_timestamp`, `idx_license_plate` (sparse), `idx_agent_detections_ttl` (TTL 30d).
**Writers**: outbox worker projection only (DW-03 resolved Phase 7).

### `decision_events`
Document shape:
```json
{
  "decision_id": "dec_gate1_...",
  "truck_id": "TRUCK-...",
  "gate_id": 1,
  "appointment_id": 42,
  "agent_detections": { "truck_detection": {}, "license_plate_detection": {}, "hazmat_detection": {} },
  "decision_engine": { "decision": "ACCEPTED", "decision_reason": "...", ... },
  "operator_decision": null,
  "final_decision": "ACCEPTED",
  "final_decision_source": "agent|operator",
  "postgres_updates": { "appointment_updated": true, "new_status": "in_process", ... },
  "timing": { ... },
  "created_at": "ISODate",
  "version": 1
}
```
**Indexes**: `idx_decision_id` (UNIQUE, partial filter `{decision_id: {$type: "string"}}`), `idx_truck_created` (compound, non-unique — BR-37 spec gap), `idx_gate_decision_created`, `idx_manual_review` (sparse), `idx_pipeline_performance`, `idx_decision_license_plate` (sparse), `idx_decision_events_ttl` (TTL 90d).
**Writers**: outbox worker projection only (DW-03, PD-02 resolved Phase 7). `persist_decision_event` retained with `DeprecationWarning`.

### `notifications`
Document shape:
```json
{
  "gate_id": 1,
  "title": "Truck Accepted",
  "message": "...",
  "type": "success|info|warning|error",
  "appointment_id": 42,
  "license_plate": "AB-12-CD",
  "read": false,
  "created_at": "ISODate"
}
```
**Writers**: `cmd_send_notification` via UoW + outbox projection (DW-01 resolved Phase 5). TTL 30d (`idx_notif_ttl`).

### `statistics_hourly`
Document shape:
```json
{
  "gate_id": 1,
  "hour_bucket": "ISODate",
  "detections": { "total": 12, "by_agent": {}, "avg_confidence": {} },
  "decisions": { "total": 10, "by_type": {} },
  "computed_at": "ISODate",
  "version": 1
}
```
**Indexes**: `idx_gate_hour_unique` (UNIQUE, `gate_id` + `hour_bucket`), `idx_stats_hourly_ttl` (TTL 400d).
**Writers**: `scripts/statistics_aggregator.py` scheduled job (DW-07 resolved Phase 5).

### `statistics_daily`
**Indexes**: `idx_stats_daily_gate_day` (UNIQUE, `gate_id` + `day_bucket`), `idx_stats_daily_ttl` (TTL 400d).

### `operator_performance`
**Indexes**: `idx_op_perf_operator_time` (`operator_id` + `computed_at` DESC), `idx_op_perf_ttl` (TTL 400d).

### `company_metrics`
**Indexes**: `idx_company_metrics_nif_time` (`company_nif` + `computed_at` DESC), `idx_company_metrics_ttl` (TTL 400d).

### `appointments_read` (CQRS read model)
**Indexes**: `idx_appts_read_appointment_id` (UNIQUE), `idx_appts_read_driver_status` (`driver_license` + `status`).
**Status**: index created (Phase 9); outbox projection to populate collection is pending (no `AppointmentCreated` event emitted yet — BR-38 partial).

### `alerts_read` (CQRS read model)
Projection target for `AlertRaised` / `AlertResolved` outbox events (Phase 6).

---

## Redis Key Patterns

| Key Pattern | TTL | Operation | BR | Notes |
|-------------|-----|-----------|-----|-------|
| `dedup:event:{event_id}` | 300s | SET NX | BR-30 | Event-id dedup gate |
| `plate:{plate}:gate:{id}:tb:{bucket}` | 300s | SET NX | BR-31 | Time-window dedup for LP+gate |
| `decision:plate:{plate}:gate:{id}:tb:{bucket}` | 3600s | SETEX (JSON) | BR-32 | Decision cache |
| `appointment:{id}:details` | 1800s | SETEX (JSON) | BR-33 | Hot appointment cache |
| `lp_lookup:{plate}:appointments` | 600s | SETEX (JSON) | BR-34 | LP → appointment list |
| `counter:gate:{id}:hour:{ts}:{metric}` | 7200s | INCRBY | BR-35 | Real-time gate counters |
| `pending_review:{event_id}` | 1800s | SETEX (JSON) | BR-36 | MANUAL_REVIEW correlation; PG-backed (`pending_reviews` table); key is event_id since Phase 4 |
| `ratelimit:api:{endpoint}:{client}` | 60s | INCR | — | Rate limiting window (`redis.check_rate_limit`) |
| `session:driver:{license}` | 3600s | SETEX (JSON) | — | Driver session (moved from PG in Phase 2) |
| `session:operator:{num_worker}` | 3600s | SETEX (JSON) | — | Operator session |
| `session:manager:{num_worker}` | 3600s | SETEX (JSON) | — | Manager session |
| `stats:gate:{id}:{date}` | 30s | SETEX (JSON) | — | Stats endpoint cache |

---

## Related Docs

- [`ARCHITECTURE_OVERVIEW.md`](ARCHITECTURE_OVERVIEW.md)
- [`KNOWN_DEVIATIONS.md`](KNOWN_DEVIATIONS.md)
- [`infrastructure/persistence/sql_models.md`](infrastructure/persistence/sql_models.md)
- [`infrastructure/persistence/redis.md`](infrastructure/persistence/redis.md)
- [`infrastructure/persistence/mongo.md`](infrastructure/persistence/mongo.md)
